import { SQLExrcuters } from "components/pages/SQLExecuters";
import {RecoilRoot} from "recoil"
function App() {
  return (
    <RecoilRoot>
      <SQLExrcuters/>
    </RecoilRoot>
  );
}

export default App;
